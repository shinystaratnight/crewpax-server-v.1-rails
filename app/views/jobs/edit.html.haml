.page-header
  %h1 Edit job

= simple_form_for @job, url: job_path(@job, secret: params[:secret]), html: { role: 'form' } do |f|
  = render 'form', f: f

  %p
    = link_to 'Remove', job_path(@job, secret: params[:secret]), method: 'DELETE', class: 'btn btn-block btn-danger btn-lg'
